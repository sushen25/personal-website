{
  "scripts": {
    "dev": "serverless offline",
    "dev:streaming": "source venv/bin/activate && python -m uvicorn src.main:app --host 0.0.0.0 --port 8001 --reload",
    "start": "source venv/bin/activate && python -m uvicorn src.main:app --host 0.0.0.0 --port 8001"
  },
  "devDependencies": {
    "serverless-offline": "^14.4.0"
  }
}