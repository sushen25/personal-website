services:
  frontend:
    build:
      context: frontend
      target: development
    ports:
      - 3000:3000
    volumes:
      - ./frontend/src:/code/src
      - ./frontend/tailwind.config.js:/code/tailwind.config.js
    environment:
      - DISABLE_ESLINT_PLUGIN=true                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                
